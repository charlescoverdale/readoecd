% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/education.R
\name{get_oecd_education}
\alias{get_oecd_education}
\title{Get OECD education expenditure data}
\usage{
get_oecd_education(countries = "all", start_year = 1990, refresh = FALSE)
}
\arguments{
\item{countries}{Character vector of ISO 3166-1 alpha-3 country codes, or
\code{"all"} for all available OECD members. Defaults to \code{"all"}. Run
\code{\link[=list_oecd_countries]{list_oecd_countries()}} to see available codes.}

\item{start_year}{Numeric. Earliest year to include. Defaults to \code{1990}.}

\item{refresh}{Logical. If \code{TRUE}, re-download even if a cached copy
exists. Defaults to \code{FALSE}.}
}
\value{
A data frame with columns:
\describe{
\item{country}{ISO 3166-1 alpha-3 country code (character)}
\item{country_name}{English country name (character)}
\item{year}{Calendar year (integer)}
\item{series}{\code{"EDU_EXPENDITURE"} (character)}
\item{value}{Total education expenditure as a share of GDP (numeric)}
\item{unit}{\code{"\% of GDP"} (character)}
}
}
\description{
Downloads (and caches) total education expenditure as a share of GDP for
OECD member countries from the OECD Education at a Glance (EAG) UOE
Finance database.
}
\details{
Education expenditure covers spending on educational institutions across
all levels of education (ISCED 0--8), from all public and private sources,
expressed as a percentage of GDP.
}
\examples{
\donttest{
op <- options(readoecd.cache_dir = tempdir())
edu <- try(get_oecd_education(c("AUS", "GBR", "USA"), start_year = 2000))
if (!inherits(edu, "try-error")) head(edu)
options(op)
}

}
\seealso{
Other social indicators: 
\code{\link{get_oecd_health}()},
\code{\link{get_oecd_inequality}()}
}
\concept{social indicators}
