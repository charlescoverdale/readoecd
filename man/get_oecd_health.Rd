% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health.R
\name{get_oecd_health}
\alias{get_oecd_health}
\title{Get OECD health expenditure data}
\usage{
get_oecd_health(countries = "all", start_year = 1990, refresh = FALSE)
}
\arguments{
\item{countries}{Character vector of ISO 3166-1 alpha-3 country codes, or
\code{"all"} for all available OECD members. Defaults to \code{"all"}. Run
\code{\link[=list_oecd_countries]{list_oecd_countries()}} to see available codes.}

\item{start_year}{Numeric. Earliest year to include. Defaults to \code{1990}.}

\item{refresh}{Logical. If \code{TRUE}, re-download even if a cached copy
exists. Defaults to \code{FALSE}.}
}
\value{
A data frame with columns:
\describe{
\item{country}{ISO 3166-1 alpha-3 country code (character)}
\item{country_name}{English country name (character)}
\item{year}{Calendar year (integer)}
\item{series}{\code{"HEALTH_EXPENDITURE"} (character)}
\item{value}{Total health expenditure as a share of GDP (numeric)}
\item{unit}{\code{"\% of GDP"} (character)}
}
}
\description{
Downloads (and caches) total current health expenditure as a share of GDP
for OECD member countries from the System of Health Accounts (SHA) database.
}
\details{
Health expenditure covers all spending on health care goods and services
across all financing sources (government, compulsory insurance, voluntary
insurance, and out-of-pocket payments), measured as a percentage of GDP at
current prices.
}
\examples{
\donttest{
op <- options(readoecd.cache_dir = tempdir())
health <- get_oecd_health(c("AUS", "GBR", "USA"), start_year = 2000)
head(health)
options(op)
}

}
\seealso{
Other social indicators: 
\code{\link{get_oecd_education}()},
\code{\link{get_oecd_inequality}()}
}
\concept{social indicators}
